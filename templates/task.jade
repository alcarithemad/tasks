extends layout

block content
	p Description:
		b &nbsp#{task['description']}
	p Due:
		b &nbsp#{due}
	p project:
		b &nbsp#{task['project'] if 'project' in task else 'no project'}
	a(href="/action?action=done&id=#{task['id']}") Complete

